# Acme DevOps Toolkit dependencies (kept in sync with poetry.lock).
#
# Versions are pinned OLD ON PURPOSE so xgrep's dependency scan (SCA) reports
# known CVEs and End-of-Life packages against Mondoo's vulnerability database.
# xgrep reads the *resolved* versions from poetry.lock; this file mirrors them
# for readers who don't use Poetry. Do not bump — the outdated pins are the demo.

Flask==0.12.2
Werkzeug==0.14.1
Jinja2==2.10
requests==2.19.1
urllib3==1.24.1
PyYAML==5.1
PyJWT==1.5.0
cryptography==2.3
